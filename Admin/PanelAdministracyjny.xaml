<Window x:Class="Admin.PanelAdministracyjny"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
<<<<<<< c1407db69e3b8a5a755e7869729ab2dc9c539300
        Title="Panel administracyjny"
        Width="1010"
        Height="400"
        Icon="palmTree.ico"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="0,0,0,340">
        <Menu>
            <MenuItem Header="_Menu">
                <MenuItem Click="MenuItemOProgramie" Header="O programie" />
                <MenuItem Click="MenuItemWyloguj" Header="_Wyloguj" />
            </MenuItem>
            <MenuItem Header="_Edytuj">
                <MenuItem Command="Copy" Header="_Kopiuj" />
                <MenuItem Command="Cut" Header="Wytnij" />
                <MenuItem Command="Paste" Header="_Wklej" />
            </MenuItem>
            <MenuItem Header="_Rezerwacje">
                <MenuItem Click="Usun_Rezerwacje" Header="Usun rezerwacje" />
            </MenuItem>
        </Menu>
        <TabControl Margin="0,30,0,-345">
            <TabItem Header="Rezerwacje">
                <Grid>
                    <ListView Name="Rezerwacje"
                              Height="230"
                              Margin="20,20,0,0"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Top"
                              Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn DisplayMemberBinding="{Binding NumerRezerwacji}" Header="Numer rezerwacji" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding NazwaWycieczki}"
                                                Header="Nazwa wycieczki" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Imie}"
                                                Header="Imię" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Nazwisko}"
                                                Header="Nazwisko" />
=======
        Title="Panel administracyjny" Height="400" Width="1010" Icon="palmTree.ico" WindowStartupLocation="CenterScreen">
    <Grid Margin="0,0,0,340">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="172*"/>
            <ColumnDefinition Width="329*"/>
        </Grid.ColumnDefinitions>
        <Menu Grid.ColumnSpan="2">
            <MenuItem Header="_Menu">
                <MenuItem Header="O programie" Click="MenuItemOProgramie"/>
                <MenuItem Header="_Wyloguj" Click="MenuItemWyloguj"/>
            </MenuItem>
            <MenuItem Header="_Edytuj">
                <MenuItem Header="_Kopiuj" Command="Copy"/>
                <MenuItem Header="Wytnij" Command="Cut"/>
                <MenuItem Header="_Wklej" Command="Paste"/>
            </MenuItem>
        </Menu>
        <TabControl Margin="0,30,0,-345" Grid.ColumnSpan="2" SelectionChanged="TabControl_SelectionChanged">
            <TabItem Header="Rezerwacje">
                <Grid>
                    <ListView Height="230" Margin="20,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Name="Rezerwacje" Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Numer rezerwacji" DisplayMemberBinding="{Binding NumerRezerwacji}"/>
                                <GridViewColumn Header="Nazwa wycieczki" Width="120" DisplayMemberBinding="{Binding NazwaWycieczki}"/>
                                <GridViewColumn Header="Imię" Width="120" DisplayMemberBinding="{Binding Imie}"/>
                                <GridViewColumn Header="Nazwisko" Width="120" DisplayMemberBinding="{Binding Nazwisko}"/>
>>>>>>> Dodawanie Kliento
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Grid>
            </TabItem>
            <TabItem Header="Wycieczki">
                <Grid>
<<<<<<< c1407db69e3b8a5a755e7869729ab2dc9c539300
                    <ListView Name="Wycieczki"
                              Height="230"
                              Margin="20,20,0,0"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Top"
                              Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn DisplayMemberBinding="{Binding NumerWycieczki}" Header="Numer wycieczki" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding NazwaWycieczki}"
                                                Header="Nazwa wycieczki" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding DataRozpoczecia}"
                                                Header="Data rozpoczęcia" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding DataZakonczenia}"
                                                Header="Data zakończenia" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Miasto}"
                                                Header="Miasto" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Kraj}"
                                                Header="Kraj" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding CenaZaOsobe}"
                                                Header="Cena za osobę" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding LiczbaMiejsc}"
                                                Header="Liczba miejsc" />
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Width="80"
                            Margin="40,267,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Click="ButtonDodajWycieczke"
                            Content="Dodaj" />
                    <Button Width="80"
                            Margin="160,267,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Click="ButtonEdytujWycieczke"
                            Content="Edytuj" />
                    <Button Width="80"
                            Margin="280,267,0,0"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top"
                            Click="ButtonUsunWycieczke"
                            Content="Usuń" />
                </Grid>
            </TabItem>
            <TabItem Header="Klienci">
                <Grid>
                    <ListView Name="Klienci"
                              Height="230"
                              Margin="20,20,0,0"
                              HorizontalAlignment="Left"
                              VerticalAlignment="Top"
                              Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn DisplayMemberBinding="{Binding NumerKlienta}" Header="Numer klienta" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Imie}"
                                                Header="Imię" />
                                <GridViewColumn Width="120"
                                                DisplayMemberBinding="{Binding Nazwisko}"
                                                Header="Nazwisko" />
                            </GridView>
                        </ListView.View>
                    </ListView>
=======
                    <ListView Height="230" Margin="20,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Name="Wycieczki" Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Numer wycieczki" DisplayMemberBinding="{Binding NumerWycieczki}"/>
                                <GridViewColumn Header="Nazwa wycieczki" Width="120" DisplayMemberBinding="{Binding NazwaWycieczki}"/>
                                <GridViewColumn Header="Data rozpoczęcia" Width="120" DisplayMemberBinding="{Binding DataRozpoczecia}"/>
                                <GridViewColumn Header="Data zakończenia" Width="120" DisplayMemberBinding="{Binding DataZakonczenia}"/>
                                <GridViewColumn Header="Miasto" Width="120" DisplayMemberBinding="{Binding Miasto}"/>
                                <GridViewColumn Header="Kraj" Width="120" DisplayMemberBinding="{Binding Kraj}"/>
                                <GridViewColumn Header="Cena za osobę" Width="120" DisplayMemberBinding="{Binding CenaZaOsobe}"/>
                                <GridViewColumn Header="Liczba miejsc" Width="120" DisplayMemberBinding="{Binding LiczbaMiejsc}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Content="Dodaj" HorizontalAlignment="Left" Margin="40,267,0,0" VerticalAlignment="Top" Width="80" Click="ButtonDodajWycieczke"/>
                    <Button Content="Edytuj" HorizontalAlignment="Left" Margin="160,267,0,0" VerticalAlignment="Top" Width="80" Click="ButtonEdytujWycieczke"/>
                    <Button Content="Usuń" HorizontalAlignment="Left" Margin="280,267,0,0" VerticalAlignment="Top" Width="80" Click="ButtonUsunWycieczke"/>
                </Grid>
            </TabItem>
            <TabItem Header="Klienci" Height="23" VerticalAlignment="Top">
                <Grid>
                    <ListView Height="230" Margin="20,20,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Name="Klienci" Background="LightYellow">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Numer klienta" DisplayMemberBinding="{Binding NumerKlienta}"/>
                                <GridViewColumn Header="Imię" Width="120" DisplayMemberBinding="{Binding Imie}"/>
                                <GridViewColumn Header="Nazwisko" Width="120" DisplayMemberBinding="{Binding Nazwisko}"/>
                                <GridViewColumn Header="PESEL" Width="120" DisplayMemberBinding="{Binding PESEL}"/>
                                <GridViewColumn Header="Data Urodzenia" Width="120" DisplayMemberBinding="{Binding DataUrodzenia}"/>
                                <GridViewColumn Header="Tel. Kontaktowy" Width="120" DisplayMemberBinding="{Binding Telefon}"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                    <Button Content="Dodaj" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Margin="40,267,0,0" Click="ButtonDodajKlienta"/>
>>>>>>> Dodawanie Kliento
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>